let notes = [];
/*note = prompt("Quel est la note de l'étudiant ?")
moyenne = 
*/
do {
    let noteStr=prompt("Quel est la note de l'étudiant ?");
    notes.push(parseFloat());
    note++;
    if (note <= 10) {
        alert("Malheureusement, vous devez reprendre votre année");
    }
    else{
        alert(`Bravo ! vous êtes admis en classe supérieure avec une moyenne de ${moyenne}`);
    }
} while(note)   